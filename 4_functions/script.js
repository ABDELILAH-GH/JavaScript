// function declaration
function greet(name) {
  return `Hello,${name}`;
}

// function expression
const greet = function (name) {
  return `Hello, ${name}`;
};

//arrow function
const greet = (name) => `Hello, ${name}`;
